<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Login Page</title>
    <link rel="stylesheet" href="styles.css">
</head>
     <style>
        * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    background-color: #fafafa;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.container {
    width: 350px;
    text-align: center;
}

.form-container {
    background-color: #fff;
    border: 1px solid #dbdbdb;
    padding: 20px;
    margin-bottom: 10px;
    border-radius: 12px; /* Rounded corners for the form */
}

.logo {
    width: 175px;
    margin-bottom: 20px;
}

.separator {
    position: relative;
    font-size: 13px;
    color: #8e8e8e;
    margin: 10px 0;
}

.separator::before,
.separator::after {
    content: '';
    position: absolute;
    top: 50%;
    width: 40%;
    height: 1px;
    background-color: #dbdbdb;
}

.separator::before {
    left: 0;
}

.separator::after {
    right: 0;
}

form {
    display: flex;
    flex-direction: column;
}

input {
    border: 1px solid #dbdbdb;
    padding: 10px;
    margin-bottom: 10px;
    border-radius: 5px; /* Add rounded corners to inputs */
    font-size: 14px;
    background-color: #fafafa;
    transition: all 0.3s ease;
}

input:focus {
    border-color: #0095f6;
}

.submit-btn {
    background-color: #0095f6;
    color: white;
    font-weight: bold;
    border: none;
    padding: 10px;
    margin-top: 10px;
    border-radius: 25px; /* Rounded corners for button */
    cursor: pointer;
    transition: all 0.3s ease; /* Add smooth transition */
}

.submit-btn:hover {
    background-color: #007dc1; /* Darker shade on hover */
    box-shadow: 0px 4px 15px rgba(0, 149, 246, 0.5); /* Add shadow effect */
}

p {
    font-size: 12px;
    color: #8e8e8e;
    margin-top: 10px;
}

.login-container {
    background-color: #fff;
    border: 1px solid #dbdbdb;
    padding: 20px;
    border-radius: 12px; /* Rounded corners for login box */
}

a {
    color: #0095f6;
    text-decoration: none;
    font-weight: bold;
    transition: color 0.3s ease;
}

a:hover {
    color: #007dc1; /* Darker link color on hover */
}

.forgot-password {
    color: #0095f6;
    text-decoration: none;
    display: block;
    margin-top: 15px;
}

.forgot-password:hover {
    color: #007dc1;
}

.error{
    color: red;
    font-size: 20px;
}

     </style>
<body>
    <div class="container">
        <div class="form-container">
            <img src="https://upload.wikimedia.org/wikipedia/commons/a/a5/Instagram_icon.png" alt="Instagram Logo" class="logo">
            <% if(error.length > 0){ %>
                <p class="error">
                    <%= error %>
                </p>
            <% } %>
            <form action="/login" method="post">
                <input name="username" type="text" placeholder="Username">
                <input name="password" type="password" placeholder="Password">
                <button type="submit" class="submit-btn">Log In</button>
            </form>
            <div class="separator">OR</div>
            <a href="#" class="forgot-password">Forgot password?</a>
        </div>
        <div class="login-container">
            <p>Don't have an account? <a href="/">Sign up</a></p>
        </div>
    </div>
</body>
</html>
